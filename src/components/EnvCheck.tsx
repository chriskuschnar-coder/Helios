import React, { useEffect, useState } from 'react'
import { AlertCircle, CheckCircle, Key, Globe } from 'lucide-react'

export function EnvCheck() {
  // Don't show anything - environment variables are handled in deployment
  return null
}